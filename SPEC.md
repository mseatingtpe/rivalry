# rivalry — SPEC

> 這份是產品規格的 single source of truth。實作與這份衝突時，以這份為準。
> 進度看 [PROGRESS.md](./PROGRESS.md)，接手鐵則看 [CLAUDE.md](./CLAUDE.md)。

## 這是什麼

《對手》是一副給兩個人面對面玩的問答卡牌，沒有輸贏。它是一件安靜、克制、偏文學的數位物件，不是一個功能型 App。任何裝飾都要為「一張紙牌被翻開」這件事服務。作者署名為電子報《無用之用 Master of None》。

核心機制：**揭幕互動**——牌疊升起、攤開成弧、玩家手選牌背抽牌。抽牌是「儀式性」的：牌背無字無記號，不論點哪一張，拿到的都是牌堆順序的下一張。手選帶來「交付給隨機」的儀式感，但幕序與排序約束完全不受影響。

## 已確定的決策（不可自行更動）

由 Sunny 拍板。要改必須先問，不要在實作中「順手優化」掉。

| # | 項目 | 決定 |
|---|------|------|
| 1 | 卡片文字 | 下方「卡片內容」全部逐字使用：一字不改、不增不刪、不潤飾，全形標點。幾張卡刻意沒有句尾標點，保留原樣 |
| 2 | 儀式性抽牌 | 點弧中任一張牌背，實際拿到的都是牌堆順序的下一張。不可讓點選位置真的決定抽到哪張 |
| 3 | 介面文字 | 降到最低：只有「照三幕／亂序／下一張／再洗一次」。不要任何教學提示、鼓勵語、說明文字 |
| 4 | 字型 | **mono、電腦感**（Source Han Mono TC，OFL，子集化 base64 內嵌）。2026-08-16 推翻原稿的 Noto Serif TC |
| 5 | 單色 | 米白紙底、近黑字、單一色相的明度變化。不引入第二個顏色、無 3D 翻牌 |
| 6 | 零外部請求 | 不載外部圖片、不打 API、不用 localStorage／sessionStorage／後端。狀態存記憶體 |
| 7 | Repo 公開 | public（GitHub Pages 免費版需要）。2026-08-16 推翻原本的 private |
| 8 | 空白卡機制 | 原版「玩家可寫入新題」已移除。要重新加入需另行設計 |

## 使用者與情境

兩個人面對面，一支手機（直式優先）放在中間輪流傳遞或共看。一局 25 張，時長由玩家自己掌控。觸控為第一互動，同時支援桌機滑鼠。

## 卡片內容（逐字，不可更動）

### 開場畫面

- 標題：《對手》
- 署名（標題下方，極小字）：這是根據《無用之用 Master of None》的作者在意的事寫成的牌組。
- 題詞（僅一句，非清單）：所有答案只代表今天的你。明天可以翻案。
- 兩個模式按鈕：照三幕、亂序

### 題目牌（每張上緣以極小字標幕別：辨識／對手／代價／留白）

**辨識（12 張，幕內固定順序）**

1. 此時此刻最喜歡的書
2. 如果可以穿越任何時空，最想看的演唱會是？
3. 最近最喜歡的瞬間
4. 不提職業與職稱，你會如何介紹你自己？
5. 說一件你最近改變心意的事。以前怎麼想，現在怎麼想。
6. 哪一件別人眼中的「無用」之事你很喜歡？
7. 你有一種不被大眾理解、極其私人的「偏愛」嗎？
8. 你上一次覺得被看見，是哪個瞬間？說畫面。
9. 哪一個童年瞬間，形塑了現在的你？
10. 說一次你決定信任一個人的瞬間。你看到了什麼？
11. 一個你很喜歡、但決定不擁有的東西。
12. 你最近一次說謊，是為了保護什麼？

**對手（5 張，幕內固定順序）**

1. 朋友如何形容你時，你會很開心
2. 問我一個你猜沒有人問過我的問題。
3. 你覺得我誤解你最深的地方是哪裡？
4. 數到三，同時回答：對方身上一個你羨慕的能力。
5. 用你專業裡的一個詞，形容我們現在的關係。

**代價（6 張，幕內固定順序）**

1. 你上一次覺得自己「沒有才華」是什麼時候？
2. 你會奮不顧身的事
3. 你為愛付過最貴的一次代價是什麼
4. 你預設人會怎麼讓你失望？
5. 我身上有哪一部分，是因為認識你才長出來的？
6. 說一件你已經原諒、但沒說出口的事。

**留白（1 張）**

1. 跳過這輪。話語以外的空白，不一定需要被填滿。

### 終卡（固定壓底，不參與洗牌）

我們不可能真正理解另外一個人。所以，約下一次。

牌組合計 24 張題目牌 + 1 張終卡 = 25 張。

## 遊戲機制

### 兩種模式

- **照三幕**：題目依 辨識 → 對手 → 代價 三幕順序推進，各幕幕內順序固定（即上列順序），不可打亂。留白牌隨機洗入三幕之間的任意位置。
- **亂序**：辨識、對手、代價、留白全部混洗成一疊，順序完全隨機。

### 留白牌歸屬（照三幕模式）

留白牌在「幕」的歸屬上，屬於它後面第一張非留白題目所在的那一幕；若其後已無題目牌，則歸最後一幕。目的是讓每一幕在牌堆中維持連續、不被留白切斷。

**實作詮釋（2026-08-16 定調）**：在 23 張題目序列的 24 個空隙中均勻隨機挑一個插入，歸屬按上述規則計算。結果是某一幕的弧為 13／6／7 張其中之一。

### 終卡

- 永遠是最後一張，永不參與洗牌。
- 一開始就以牌背形式擺在桌面尾端，整場可見，走到最後才被翻開。

### 前進規則

- 只能往前，不能回頭。
- 前進方式：點當前卡片、或點桌面上的牌、或「下一張」按鈕、或在卡片上向左滑。
- 右下角極小字進度 n／總數。

### 終卡之後

只留一個小連結「再洗一次」，回到開場重選模式，重新洗牌開局。

### 揭幕互動（核心體驗）

**照三幕模式**

1. 選完模式，桌面上安靜擺著三疊牌背（依序標示辨識、對手、代價）與桌尾一張獨立的終卡牌背。舞台（上方翻牌區）此時是空的。
2. 只有「當前該打開的那一疊」是亮的，還沒輪到的疊呈半透明等待狀態。
3. 點亮著的疊 → 那疊升起、攤開成一道微彎的弧（fan）。這個「攤開」就是揭幕動作，一局發生三次。
4. 攤開後停住，讓玩家看清這一幕有幾張牌在等（不自動抽第一張）。
5. 點弧中任一張牌背 → 那張升起、放大、淡入成為舞台上的題目卡；其餘牌背滑動補位，弧慢慢變短。
6. 一幕的弧抽完清空 → 下一疊亮起。三幕走完，桌面只剩終卡牌背，此時它才亮起；抽它，遊戲結束。

**亂序模式**

不設三疊。選完模式後，整副牌背直接一次攤開成一道長弧。之後抽牌行為同上。

## 範圍

### 這一期要做

- 開場畫面、照三幕／亂序兩模式、揭幕互動、儀式性抽牌、終卡、再洗一次。
- 單一 HTML 檔產物，零外部請求，GitHub Pages 上線。
- 字型子集化 base64 內嵌。

### 這一期明確不做

- 玩家寫入新題的空白卡機制（原版有，此版已移除）。
- 教學提示、鼓勵語、任何說明文字。
- 3D 翻牌、第二個顏色、瀏覽器儲存、後端、帳號、分享功能。

## 技術決定

| 項目 | 決定 | 理由 |
|------|------|------|
| 語言 / Runtime | 單一 HTML 檔 + vanilla JS（源檔在 `src/`，build 產出 root `index.html`） | 零依賴零 build framework；25 張牌的狀態機不需要 React |
| 字型 | Source Han Mono TC 子集化 → woff2 → base64 內嵌；fallback `ui-monospace, monospace` | Sunny 要 mono 電腦感；OFL 可內嵌；零外部請求 |
| 資料儲存 | 記憶體（JS 變數） | 規格明令不用瀏覽器儲存與後端 |
| 部署 | GitHub Pages（Deploy from a branch：`main` / root） | 單檔靜態站最短路徑；`mseatingtpe` token 無 `workflow` scope，不走 Actions |
| 認證 | 無 | 純靜態，無使用者概念 |

## 資料模型

```js
// src/deck.js — 實作端 single source of truth（卡片文字以本 SPEC 為準）
card = { act: '辨識'|'對手'|'代價'|'留白'|'終卡', text: string }
buildDeck(mode /* 'acts' | 'shuffle' */) => card[25]  // 終卡永遠在 index 24
```

## 驗收條件

硬性門檻（沒過不准往下走）；各 Phase 驗收見 [PROGRESS.md](./PROGRESS.md)。

- `node tools/verify-deck.mjs` 生成牌堆 1000 次，每次皆成立：
  - 恰 25 張，終卡永遠最後一張。
  - 各幕張數：辨識 12、對手 5、代價 6、留白 1。
  - 照三幕模式下，留白歸幕後三幕各自連續、不重疊、不留空隙，序為 辨識 → 對手 → 代價。
  - 每幕內部題目相對順序與本 SPEC 完全一致。
- `index.html` 單檔以 `file://` 開啟可完整遊玩，DevTools Network 零外部請求。
- 手機直式跑完照三幕與亂序各一局：無跳張、無回頭、無教學文字；快速連點不會一次前進兩張。
- `prefers-reduced-motion` 下裝飾動畫收斂。
